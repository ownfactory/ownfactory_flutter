name: ownfactory_flutter
description: Own Factory project mobile application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3

  # logger
  loggy: ^2.0.1
  flutter_loggy: ^2.0.1

  # DI
  get_it: ^7.2.0
  injectable: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # DI
  injectable_generator: ^1.5.2
  build_runner: ^2.1.4

  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
